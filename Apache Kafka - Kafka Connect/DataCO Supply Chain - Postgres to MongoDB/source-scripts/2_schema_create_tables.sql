\c dataco_scd_db;

CREATE TABLE IF NOT EXISTS dataco_scd_table (
    transaction_type VARCHAR(18),
    actual_days_to_ship INTEGER,
    scheduled_days_to_ship INTEGER,
    benefit_per_order FLOAT,
    sales_per_customer FLOAT,
    delivery_status VARCHAR(20),
    late_delivery_risk INTEGER,
    category_id INTEGER,
    category_name VARCHAR(36),
    customer_city VARCHAR(24),
    customer_country VARCHAR(24),
    customer_email VARCHAR(18),
    customer_fname VARCHAR(36),
    customer_id INTEGER,
    customer_lname VARCHAR(36),
    customer_password VARCHAR(18),
    customer_segment VARCHAR(12),
    customer_state VARCHAR(8),
    customer_street VARCHAR(60),
    customer_zipcode INTEGER,
    department_id INTEGER,
    department_name VARCHAR(30),
    latitude FLOAT,
    longitude FLOAT,
    market VARCHAR(15),
    order_city VARCHAR(36),
    order_country VARCHAR(36),
    order_customer_id INTEGER,
    order_date VARCHAR(20),
    order_id INTEGER,
    order_item_cardprod_id INTEGER,
    order_item_discount FLOAT,
    order_item_discount_rate FLOAT,
    order_item_id INTEGER,
    order_item_product_price FLOAT,
    order_item_profit_ratio FLOAT,
    order_item_quantity INTEGER,
    sales FLOAT,
    order_item_total FLOAT,
    order_profit_per_order FLOAT,
    order_region VARCHAR(20),
    order_state VARCHAR(36),
    order_status VARCHAR(20),
    order_zipcode INTEGER,
    product_card_id INTEGER,
    product_category_id INTEGER,
    product_desc VARCHAR(6),
    product_image VARCHAR(120),
    product_name VARCHAR(60),
    product_price FLOAT,
    product_status INTEGER,
    shipping_date VARCHAR(20),
    shipping_mode VARCHAR(20)
);