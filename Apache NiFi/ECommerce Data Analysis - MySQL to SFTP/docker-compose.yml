services:
  sftp:
    image: atmoz/sftp
    container_name: sftp
    hostname: sftp
    platform: linux/amd64
    ports:
      - "2222:22"
    volumes:
      - sftp_data:/home/nifi/upload
    environment:
      SFTP_USERS: "nifi_user:nifi_password:1001"
      SFTP_UID: "1001"
    command: nifi_user:nifi_password:1001::upload
    restart: unless-stopped
    networks:
      - project_network
    
  mysql:
    image: mysql:8.0
    container_name: mysql
    hostname: mysql
    restart: always
    command: --local-infile=1
    environment:
      MYSQL_ROOT_PASSWORD: mysql-root-pw
      MYSQL_USER: mysql
      MYSQL_PASSWORD: mysql
      MYSQL_DATABASE: ecomm_data_analysis_db_mysql
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql_scripts:/docker-entrypoint-initdb.d
      - ./mysql_scripts/customer_dim.csv:/docker-entrypoint-initdb.d/customer_dim.csv
      - ./mysql_scripts/trans_dim.csv:/docker-entrypoint-initdb.d/trans_dim.csv
      - ./mysql_scripts/time_dim.csv:/docker-entrypoint-initdb.d/time_dim.csv
      - ./mysql_scripts/store_dim.csv:/docker-entrypoint-initdb.d/store_dim.csv
      - ./mysql_scripts/item_dim.csv:/docker-entrypoint-initdb.d/item_dim.csv
      - ./mysql_scripts/fact_table.csv:/docker-entrypoint-initdb.d/fact_table.csv
    networks:
      - project_network

  zookeeper:
    image: bitnami/zookeeper:3.9.2
    hostname: zookeeper
    container_name: zookeeper
    restart: on-failure
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - project_network
    ports:
      - '2181:2181'

  nifi-registry:
    hostname: nifi-registry
    container_name: nifi-registry
    image: 'apache/nifi-registry:1.27.0'
    restart: on-failure
    ports:
      - 18080:18080
    environment:
      - LOG_LEVEL=INFO
      - NIFI_REGISTRY_DB_DIR=/opt/nifi-registry/nifi-registry-current/database
      - NIFI_REGISTRY_FLOW_PROVIDER=file
      - NIFI_REGISTRY_FLOW_STORAGE_DIR=/opt/nifi-registry/nifi-registry-current/flow_storage
    volumes:
      - ./nifi_registry/database:/opt/nifi-registry/nifi-registry-current/database
      - ./nifi_registry/flow_storage:/opt/nifi-registry/nifi-registry-current/flow_storage
    networks:
      - project_network

  nifi:
    hostname: nifi
    container_name: nifi
    image: apache/nifi:1.27.0
    restart: on-failure
    ports:
      - '8090:8080'
    environment:
      - NIFI_WEB_HTTP_PORT=8080
      - NIFI_CLUSTER_IS_NODE=true
      - NIFI_CLUSTER_NODE_PROTOCOL_PORT=8082
      - NIFI_ZK_CONNECT_STRING=zookeeper:2181
      - NIFI_ELECTION_MAX_WAIT=30 sec
      - NIFI_SENSITIVE_PROPS_KEY='12345678901234567890A'
      - NIFI_HEAP_INIT=4g
      - NIFI_HEAP_MAX=8g
      - NIFI_JVM_OPTS=-Dorg.apache.nifi.content.repository.archive.max.retention=10GB
      - NIFI_JVM_OPTS=-Dnifi.provenance.repository.max.storage=10GB
      - NIFI_JVM_OPTS=-Dnifi.flowfile.repository.rocksdb.block.cache.size=512MB
      - NIFI_JVM_OPTS=-Dnifi.flowfile.repository.rocksdb.write.buffer.size=32MB
    healthcheck:
      test: "${DOCKER_HEALTHCHECK_TEST:-curl localhost:8091/nifi/}"
      interval: "15s"
      timeout: "3s"
      start_period: "5s"
      retries: 5
    volumes:
      - nifi_data:/opt/nifi/nifi-current/data
      - ./nifi/database_repository:/opt/nifi/nifi-current/database_repository
      - ./nifi/flowfile_repository:/opt/nifi/nifi-current/flowfile_repository
      - ./nifi/content_repository:/opt/nifi/nifi-current/content_repository
      - ./nifi/provenance_repository:/opt/nifi/nifi-current/provenance_repository
      - ./nifi/state:/opt/nifi/nifi-current/state
      
      - ./jar_files/mysql-connector-j-9.2.0.jar:/opt/nifi/nifi-current/lib/mysql-connector-j-9.2.0.jar

      - ./nifi/conf:/opt/nifi/nifi-current/conf
      - ./nifi/logs:/opt/nifi/nifi-current/logs
    networks:
      - project_network

volumes:
  nifi_data:
  mysql_data:
  sftp_data:

networks:
  project_network:
    driver: bridge

# -------------------------------------------------
# Startup notes:

# docker compose up -d

# docker ps -a

# docker cp <docker_container_id>:/opt/nifi/nifi-current/conf ./nifi/

# Uncomment the last line in the nifi volumes after copying the /conf directory 
# from the container to your local directory to persist NiFi flows.

# docker compose up --force-recreate -d

# http://localhost:18080/nifi-registry/
# http://localhost:8090/nifi/
# -------------------------------------------------