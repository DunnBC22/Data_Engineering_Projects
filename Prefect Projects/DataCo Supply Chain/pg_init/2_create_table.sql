\c dataco_sc_pg_db;

CREATE TABLE dataco_sc_pg_table (
    transaction_type VARCHAR(12),
    actual_days_to_ship INTEGER,
    scheduled_days_to_ship INTEGER,
    benefit_per_order FLOAT,
    sales_per_customer FLOAT,
    delivery_status VARCHAR(25),
    late_delivery_risk INTEGER,
    category_id INTEGER,
    category_name VARCHAR(30),
    customer_city VARCHAR(30),
    customer_country VARCHAR(16),
    customer_email VARCHAR(12),
    customer_fname VARCHAR(16),
    customer_id BIGINT,
    customer_lname VARCHAR(18),
    customer_password VARCHAR(12),
    customer_segment VARCHAR(16),
    customer_state VARCHAR(8),
    customer_street VARCHAR(40),
    customer_zipcode INTEGER,
    department_id INTEGER,
    department_name VARCHAR(24),
    latitude FLOAT,
    longitude FLOAT,
    market VARCHAR(18),
    order_city VARCHAR(40),
    order_country VARCHAR(40),
    order_customer_id BIGINT,
    order_date VARCHAR(21),
    order_id INTEGER,
    order_item_cardprod_id INTEGER,
    order_item_discount FLOAT,
    order_item_discount_rate FLOAT,
    order_item_id INTEGER,
    order_item_product_price FLOAT,
    order_item_profit_ratio FLOAT,
    order_item_quantity FLOAT,
    sales FLOAT,
    order_item_total FLOAT,
    order_profit_per_order FLOAT,
    order_region VARCHAR(20),
    order_state VARCHAR(42),
    order_status VARCHAR(21),
    order_zipcode INTEGER,
    product_card_id INTEGER,
    product_category_id INTEGER,
    product_desc VARCHAR(255),
    product_image VARCHAR(110),
    product_name VARCHAR(60),
    product_price FLOAT,
    product_status INTEGER,
    shipping_date VARCHAR(20),
    shipping_mode VARCHAR(20)
);